class OrderService {

	createOrder(orderCreationQuery: OrderCreationQuery): void {
		// create an order using the provided data
		// ...
	}

	cancelOrder(orderId: OrderId): void {
		// cancel the order with the given id
		// ...
	}

	markAsPaid(orderId: OrderId): void {
		// mark the order with the given id as paid
		// ...
	}

	markAsPaid(orderId: OrderId): void {
		// mark the order with the given id as paid
		// ...
	}

	calculateOrderTotal(orderId: OrderId): void {
		// calculate the total prrice of the order with the given id
		// ...
	}

}